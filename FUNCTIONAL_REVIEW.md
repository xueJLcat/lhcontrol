# 功能分析与修复记录

本次检查范围为应用功能与状态一致性，不包含安全性专项审查。检查基于仓库源码、现有测试、新增故障注入测试及 Windows 上的构建工具链；没有以真实 Lighthouse 基站执行电源或通道操作。

## 已确认并修复的问题

| 问题 | 触发条件与影响 | 实现方案 |
| --- | --- | --- |
| 状态查询没有等待上限 | Wails 的扫描状态、扫描中标志或站点列表查询不返回时，扫描恢复、外部事件恢复或命令后的状态对账一直等待；完整状态刷新也可能永久占用前端运行状态 | 在 `frontend/src/lib/backend.ts` 统一为三个缓存查询增加 10 秒等待上限；实际执行 BLE 的完整刷新使用 180 秒。超时以错误交给已有恢复逻辑，不伪造空列表或空闲状态；迟到的返回与拒绝不会重复提交 |
| 扫描恢复过早取消有效工作 | 原固定 90 秒计时早于允许的 30 秒扫描加 120 秒读取阶段，更没有覆盖排队、初始化与收尾时间 | 将异常扫描恢复触发窗口设为 360 秒，覆盖现行配置上限与后端等待预算；用户主动停止仍可立即发起 |
| 强制恢复后旧扫描结果仍有提交资格 | 原恢复只清理界面标志，没有立即推进扫描与列表版本；无后续轮询时，旧 Promise 返回的列表仍可能被接受 | 解锁前推进扫描 epoch 与列表 revision，并单独取得恢复提示的状态版本，旧结果与迟到提示均不能覆盖恢复后的状态 |
| 外部扫描接管后本地状态无法释放 | 本地扫描已返回站点列表、但最终状态查询仍在等待时，外部事件推进了扫描 epoch；原 finally 只检查该 epoch，跳过清理后一直显示扫描中 | 增加本地运行身份，分别管理结果提交和运行状态清理。外部结果可以取代旧结果，本地运行仍能清理自己；已启动的后继本地扫描不会被旧 finally 清除 |
| 单站修改通道后同伴仍显示冲突 | 单站响应仅更新被修改的基站，留在列表中的同伴继续携带旧 `channelConflict` | `FleetState` 在列表提交、局部合并及有效期到期后，按当前完整列表重新计算每个站点的冲突标记；列表替换也经过同一入口 |
| 通道读数过期后仍被标成确定冲突 | 通道图原使用较长的显示有效期及缓存标记，操作有效期已到时仍可能标红，与通道修改校验不一致 | 通道图使用操作有效的读数计算冲突，同时保留显示用通道；统一占用/未知判断需要满足在场、无漏扫、无在场不确定性及扫描新鲜度 |
| 保存失败的补偿读取覆盖了更新中的新设置 | 第一次保存失败后，旧值补偿读取尚未完成时用户再次编辑；旧快照或旧错误会覆盖新输入，界面与随后保存结果不一致 | 补偿读取仍在同一串行保存队列中执行，但只有在保存版本仍是当前版本时才应用读取值或错误；较新的乐观值由后续保存继续负责 |
| 无别名设备的通道冲突消息缺少设备名称 | 未设置别名时显示名解析会返回未重命名结果，冲突错误直接使用空字符串，用户无法知道哪个基站占用了通道 | 仅在确实有非空别名时使用配置显示名，否则回退到扫描快照中的工厂名称 |
| 重新连接后的初始读仍携带旧设备信息错误 | 断线重连期间发现流程已经成功清除旧的设备信息读取错误，但初始状态读取在发现前捕获旧错误，随后错误地安排一次元数据恢复 | 将元数据错误快照放到连接与发现成功之后读取；已连接快速路径仍使用当前缓存，新的发现结果决定本次初始读是否报告错误 |

## 方案取舍

- 复用现有分层结构，在后端绑定适配层集中限制只读查询，避免每个控制器各自遗漏恢复分支；不对电源或通道写入增加自动重试。
- 扫描异常恢复采用覆盖当前最大配置的固定上限，避免另一次异步设置读取成为启动依赖。代价是默认配置下真正挂起的扫描需要更久才触发自动恢复；手动停止流程仍保留独立等待预算。后续若提高后端配置上限，应同步调整此上限。
- 冲突是多个站点共同决定的属性，应由完整列表派生，而不是把各次单站响应中的标记直接拼接起来。过期后的旧通道仍可用于显示，但不能作为确定冲突证据。
- 结果版本控制与运行清理身份分开：前者拒绝陈旧数据，后者保证各次运行最终释放自己拥有的状态。
- 设置保存的补偿读取属于旧保存的收尾动作，必须服从保存版本；设备信息错误则相反，必须在发现完成后取快照，避免把已恢复的旧错误继续向上游传播。

## 检查覆盖

| 领域 | 本次检查内容与验证边界 |
| --- | --- |
| 扫描与恢复 | 扫描准备、取消、读取预算、终态、前端恢复计时、外部扫描事件、版本控制；新增长扫描、迟到结果和交接回归 |
| 单站及批量电源 | 缓存状态校验、启动过渡态、命令发送/确认区别、超时与取消的部分结果；现有 Go 与前端动作测试验证 |
| 通道与设备信息 | 通道冲突、未知通道、能力刷新、读回确认与前端冲突派生；新增局部更新及过期回归 |
| 自动休眠 | 进程观察、触发债务、目标/设置替换、运行中取消及事件状态；现有 watcher、应用与前端事件测试验证 |
| 配置及 HTTP | 加载与保存恢复、设置校验、监听地址切换、错误映射、操作事件；现有配置/API 测试及设置并发保存回归验证 |
| 生命周期及 Windows | 单实例、关闭、扫描/读数排空、适配器恢复及内置 WinRT 蓝牙分支；Go 测试、竞态检测及无硬件脚本自检验证 |

未在上述其余领域确认新的需要修改的功能缺陷，因此没有为扩大改动范围重写已验证的实现。自动化通过不能证明不存在所有缺陷，尤其不能代替真实 BLE 时序与硬件回读验证。

## 回归验证

新增测试覆盖缓存查询挂起、迟到拒绝、成功与同步异常时的计时器清理、长 BLE 刷新等待上限、150 秒有效扫描、扫描废弃结果、本地/外部扫描交接、局部通道更新、有效期到期、设置保存失败后的并发编辑及无别名冲突提示。

三个既有 UI 测试改为明确等待初始扫描完成后再注入下一轮故障，避免在站点卡片已出现、最后状态查询尚未结束时误消耗下一轮的模拟响应。启动延迟测试使用 5 秒延迟，保持在新的缓存查询等待上限以内。

最终验证结果：

- `scripts/test.ps1` 通过：锁定依赖重新安装、Svelte 检查 0 错误/0 警告、39 个测试文件共 541 项前端测试通过、前端生产构建、全部 Go 主模块与内置蓝牙模块测试通过。
- `go vet ./...` 与 `go test -race ./...` 通过。
- `scripts/hardware-smoke.ps1 -SelfTest` 通过。
- Windows amd64 生产构建通过，生成 `build/bin/lhcontrol.exe`。
- `git diff --check` 通过。

完整测试与构建日志分别记录于 `build/verification/functional-review-tests.log` 和 `build/verification/functional-review-build.log`。真实基站的连续扫描、三种电源状态和通道回读仍需使用仓库硬件验收流程验证。
